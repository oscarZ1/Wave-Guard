# Homebrew Postgres. Change the port to match your install (5432 is the usual default).
DATABASE_URL=postgres://YOUR_USER@localhost:5433/waveguard
PORT=3000

# P1 only: plain-language explanations. Never put this key in the extension.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
